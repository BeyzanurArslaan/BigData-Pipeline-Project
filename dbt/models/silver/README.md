# Silver Models

Silver models are the cleaned, typed, and deduplicated business-ready layer.

This layer is intended for entity standardization, conformed dimensions, and reusable transformation logic that prepares data for the gold marts.

Silver models also expose order-grain payment aggregates and order-item support fields that the future Gold marts can reuse without rebuilding the business logic.
